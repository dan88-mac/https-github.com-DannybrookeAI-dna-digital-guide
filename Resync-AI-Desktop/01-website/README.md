# Website source (not duplicated)

The full Next.js SaaS lives at the repo root:

```text
../../resync-ai
```

`run.bat` / `run.sh` in the Desktop pack now install and start that folder.

Do **not** copy the entire app here again — it caused duplication and drift.
